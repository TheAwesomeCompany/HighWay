<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.StatusBarTranslucent" parent="@style/Theme.AppCompat.NoActionBar"/>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ItemName" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">#2e2b2b</item>
    </style>

    <style name="ItemAddress" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">#2e2b2b</item>
    </style>

    <style name="ItemDeliveryTime" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/grey</item>
    </style>

</resources>
